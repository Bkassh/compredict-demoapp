apiVersion: apps/v1
kind: Deployment
metadata:
  name: compredictprod
  namespace: flux-system
  labels:
    name: banana-deploy
spec:
  selector:
    matchLabels:
      app: banana
  replicas: 1
  template:
    metadata:
      labels:
        app: banana
    spec:
      containers:
      - name: banana-app
        image: hashicorp/http-echo
        imagePullPolicy: Always
        args:
        - "-text=banana"